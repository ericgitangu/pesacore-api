@page "/not-found"
@layout MainLayout

<PageTitle>Not found · PesaCore</PageTitle>

<section style="max-width:520px">
    <div class="eyebrow">404</div>
    <h1 style="font-size:clamp(2.2rem,5vw,3rem);margin:.4rem 0 .6rem">Off the ledger</h1>
    <p style="color:var(--ink-dim)">That route doesn't exist in the console.</p>
    <a href="" style="display:inline-block;margin-top:1.2rem;color:var(--mint);border-bottom:1px solid var(--mint);font-family:var(--mono);font-size:.85rem">← Back to accounts</a>
</section>
